This is to create a disk from a recovery point and create a new recovery VM from that disk.
The scripts use local variables so paste the contents of the scripts into the shell rather than running
the scripts, as variables will no survive from one script to the next if run as scripts.
